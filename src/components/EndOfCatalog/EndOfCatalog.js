import React from 'react'
import './EndOfCatalog.scss'

/**
 * Component that shows a message when maximum catalog length is reached
 * 
 * @returns {React.Element}
 */
const EndOfCatalog = () => {
    return <p className="row end-catalog">End of users catalog</p>
}

export default EndOfCatalog